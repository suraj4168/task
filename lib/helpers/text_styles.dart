import 'package:flutter/material.dart';

class AppTextStyle {
  static TextStyle textStyle(
       Color color, double size) {
    return TextStyle(
        color: color,
        fontSize: size,
    );
  }
}
